# Build artifacts only — source dirs (fork/, optimized/) are bundled
# with the artifact (~2.6 MB total) so reviewers don't need network
# access for benchmarks/lulesh/run_compare_*.sh.
*.o
*.log
make.log
